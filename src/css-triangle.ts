/**
 * CssTriangle
 * @constructor
 */
class CssTriangle {
    /**
     * generate
     * @param {object} option
     * @param {string} option.color
     * @param {number|string} option.width
     * @param {number|string} option.height
     */
    generate(option) {
        return {};
    }
}

module.exports = new CssTriangle();
